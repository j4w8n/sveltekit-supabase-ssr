/**
 * Auth validation happens in hooks.server.ts, so there's
 * no need to check anything here. Plus, we aren't returning 
 * server data to the /app page.
 * 
 * This file is optional, and only exists to trigger a server call 
 * for strict authentication checks during client side routing.
 */
